# The optional local Identity composite build configures many SDK projects.
# Match its heap requirement so Gradle does not stop during initial compilation.
org.gradle.jvmargs=-Xmx6g -XX:+UseParallelGC -Dfile.encoding=UTF-8
kotlin.compiler.execution.strategy=in-process
